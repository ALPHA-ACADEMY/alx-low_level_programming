#include "main.h"

/**
 * add - finction that adds two integers and returns the result.
 * @i: numbers that we are gonna add together.
 * @j: same as i.
 *
 * Return: the sum of two integers.
*/

int add(int i, int j)
{
	return (i + j);
}
